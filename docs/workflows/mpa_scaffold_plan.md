# MPA Scaffold Plan

This repo has been scaffolded as a Cloudflare Worker MPA using detected standalone HTML sources.

Next: run `npm install`, then `npm run deploy:dry` before pushing deployable code.
